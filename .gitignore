# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Printed from the /resume page by modules/resume-pdf.ts on every production
# build. Chrome stamps a creation time into each print, so tracking it meant a
# 172 kB binary diff whenever anything was built. CI regenerates it on deploy.
public/resume/

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example
.gstack/
