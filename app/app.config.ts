export default defineAppConfig({
  ui: {
    colors: {
      primary: "lime",
      gray: "neutral",
    },
    button: {
      defaultVariants: {
        // Set default button color to neutral
        // color: 'neutral'
      },
    },
  },
});
